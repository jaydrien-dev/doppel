import React from "react";
import { createRoot } from "react-dom/client";
import ResponseOverlay from "./pages/ResponseOverlay";

createRoot(document.getElementById("root")!).render(<ResponseOverlay />);
